// Placeholder generator: fetches the running API's OpenAPI JSON and would
// normally pipe it through openapi-typescript. Wired up once apps/api ships
// real endpoints beyond /health (Phase 1+).
console.log('SDK generation is a no-op until Phase 1 endpoints exist.');
